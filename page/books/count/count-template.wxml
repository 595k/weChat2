<template name="shopCount">
    <view class="shopCount">
        <view data-id="{{index}}" catchtap='minCount'>-</view>
        <view>{{buyCount}}</view>
        <view data-id="{{index}}" catchtap='maxCount'>+</view>
    </view>
</template>